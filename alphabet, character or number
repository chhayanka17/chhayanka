#include <stdio.h>

int main() {
    char ch;
     printf("Enter any element:\t");
     scanf("%c", &ch);
     if((ch >= 65 && ch <= 90) || (ch >= 97 && ch <= 122)){
     printf("Its an alphabet");
     } else if(ch >= 48 && ch <= 57){
     printf("Its a number\n");
     }else{
    printf("its a character");
    }
     
    return 0;
