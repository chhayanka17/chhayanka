#include <stdio.h>
int main()
{
	int i, sum, n;
	sum = 0;
	printf("Give me the n natural numbers\t");
	scanf("%d", &n);
	for(i = 0; i<=n; i++ )
	sum = sum + i;
	printf("Sum of %d natural numbers is %d\n", n, sum);
	printf("end");
	
	return 0;

}
